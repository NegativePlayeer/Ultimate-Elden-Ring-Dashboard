# Vercel / split deploy: point frontend at Render API (no trailing slash).
# Leave unset for local dev (Vite proxy) and Render monolith (same origin).
VITE_API_BASE_URL=https://ultimate-elden-ring-dashboard.onrender.com
