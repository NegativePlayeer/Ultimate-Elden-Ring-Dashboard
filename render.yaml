services:
  - type: web
    name: ultimate-elden-ring-dashboard
    runtime: python
    buildCommand: cd frontend && npm install && npm run build && pip install -r backend/requirements.txt
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
